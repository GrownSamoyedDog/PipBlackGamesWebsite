/**
 * Placeholder counts for `SiteTopNav` until live lobby / spectate / presence exists.
 * Replace usages with server or WebSocket-driven values (or a small store) when
 * those features are implemented.
 */

/** Shown after “Join Game” — number of open seek postings players can join. */
export const PLACEHOLDER_TOP_NAV_SEEK_COUNT = 0;

/**
 * Shown after “Spectate” — number of in-progress games that allow spectators.
 */
export const PLACEHOLDER_TOP_NAV_SPECTATE_GAME_COUNT = 0;

/** Shown after “Players” — online users (site-wide or in the current lobby). */
export const PLACEHOLDER_TOP_NAV_ONLINE_PLAYER_COUNT = 0;
